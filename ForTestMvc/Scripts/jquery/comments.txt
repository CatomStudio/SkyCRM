
v1.0.0
【内容模块】
0. 基本工具方法；
1. Html 选择器；
2. CSS 处理器；
3. DOM 操作器；
4. 异步操作；
5. 动态操作；
6. 事件操作；

【几个前提理解】
_$ = $ = jQuery 类——静态方法类——jQuery.extend()；
jQuery.fn = jQuery.prototype 对象（jQuery 类的实例）——原型方法类——jQuery(elem).extend()；
__proto__ 此对象（变量、对象、方法等）的类型；

【初始化入口】
var jQuery = ...
init:
var $ = jQuery;

















